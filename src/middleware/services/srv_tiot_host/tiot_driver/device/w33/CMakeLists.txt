#===============================================================================
# @brief    cmake make file
# Copyright (c) HiSilicon (Shanghai) Technologies Co., Ltd. 2023-2023. All rights reserved.
#===============================================================================
if(DEFINED CONFIG_DEV_W33)
set(
    device_include_list
    "${CMAKE_CURRENT_SOURCE_DIR}"
)

set(
    device_src_list
    ${CMAKE_CURRENT_SOURCE_DIR}/w33_dev_customize.c
    ${CMAKE_CURRENT_SOURCE_DIR}/w33_device_info.c
    ${CMAKE_CURRENT_SOURCE_DIR}/w33_service.c
)

if(DEFINED CONFIG_FILE_BY_ARRAY)
if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/firmware/w33_fw_file_array.c)
list(
    APPEND device_src_list
    ${CMAKE_CURRENT_SOURCE_DIR}/firmware/w33_fw_file_array.c
)

list(
    APPEND device_include_list
    "${CMAKE_CURRENT_SOURCE_DIR}/firmware"
)
endif()
endif()

set(TIOT_HEADER_LIST "${TIOT_HEADER_LIST}" "${device_include_list}" CACHE INTERNAL "tiot header list" FORCE)
set(TIOT_SRC_LIST "${TIOT_SRC_LIST}" "${device_src_list}" CACHE INTERNAL "tiot src list" FORCE)
endif()
